export interface Game {
  id: string;
  title: string;
  thumbnail: string;
  image: string;
  minPlayers: string;
  maxPlayers: string;
  playingTime: string;
  minAge: string;
  note: string;
  gameType: string;
}
