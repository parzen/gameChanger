export interface Game {
  id: string;
  title: string;
  imagePath: string;
  minPlayers: string;
  maxPlayers: string;
  minPlayTime: string;
  maxPlayTime: string;
  minAge: string;
  note: string;
  consider: Boolean;
  gameType: string;
  creator: string;
}
